//
// UpsertDeviceEventRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Request body for upserting a device event record (site changes, sensor starts, etc.) via the V4 API. */
public struct UpsertDeviceEventRequest: Sendable, Codable, Hashable {

    /** When the device event occurred. */
    public var timestamp: Date?
    /** UTC offset in minutes at the time of the event, for local-time display. */
    public var utcOffset: Int?
    /** Identifier of the device involved in the event. */
    public var device: String?
    /** Name of the application that submitted this record. */
    public var app: String?
    /** Upstream data source identifier. */
    public var dataSource: String?
    public var eventType: DeviceEventType?
    /** Free-text notes associated with the event (capped at 10,000 characters). */
    public var notes: String?
    /** Upstream sync identifier for deduplication. */
    public var syncIdentifier: String?

    public init(timestamp: Date? = nil, utcOffset: Int? = nil, device: String? = nil, app: String? = nil, dataSource: String? = nil, eventType: DeviceEventType? = nil, notes: String? = nil, syncIdentifier: String? = nil) {
        self.timestamp = timestamp
        self.utcOffset = utcOffset
        self.device = device
        self.app = app
        self.dataSource = dataSource
        self.eventType = eventType
        self.notes = notes
        self.syncIdentifier = syncIdentifier
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case timestamp
        case utcOffset
        case device
        case app
        case dataSource
        case eventType
        case notes
        case syncIdentifier
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(timestamp, forKey: .timestamp)
        try container.encodeIfPresent(utcOffset, forKey: .utcOffset)
        try container.encodeIfPresent(device, forKey: .device)
        try container.encodeIfPresent(app, forKey: .app)
        try container.encodeIfPresent(dataSource, forKey: .dataSource)
        try container.encodeIfPresent(eventType, forKey: .eventType)
        try container.encodeIfPresent(notes, forKey: .notes)
        try container.encodeIfPresent(syncIdentifier, forKey: .syncIdentifier)
    }
}

