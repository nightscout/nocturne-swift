//
// UpsertPumpSnapshotRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Request body for upserting a pump status snapshot via the V4 API. Mirrors PumpSnapshot minus the server-assigned fields (Id, CreatedAt, ModifiedAt, LegacyId, Mills, device FKs). */
public struct UpsertPumpSnapshotRequest: Sendable, Codable, Hashable {

    /** When the pump status was read. */
    public var timestamp: Date?
    /** UTC offset in minutes at the time of the event, for local-time display. */
    public var utcOffset: Int?
    /** Identifier of the device that produced this snapshot (e.g. pump serial number). */
    public var device: String?
    /** Name of the application that submitted this record. */
    public var app: String?
    /** Upstream data source identifier; required when SyncIdentifier is supplied. */
    public var dataSource: String?
    /** Stable per-source identifier. When paired with DataSource, re-uploading the same snapshot updates the existing record in place rather than creating a duplicate. */
    public var syncIdentifier: String?
    /** Correlation identifier shared by the APS, pump, and uploader snapshots of one loop cycle. */
    public var correlationId: String?
    /** Pump manufacturer name (e.g., \"Insulet\", \"Medtronic\", \"Tandem\"). */
    public var manufacturer: String?
    /** Pump model name (e.g., \"Omnipod DASH\", \"MiniMed 780G\"). */
    public var model: String?
    /** Insulin remaining in the reservoir (units). Omit when unknown. */
    public var reservoir: Double?
    /** Human-readable reservoir display string (e.g., \"50+ U\", \"Low\"). */
    public var reservoirDisplay: String?
    /** Pump battery level as a percentage (0-100). */
    public var batteryPercent: Int?
    /** Pump battery voltage (for devices that report voltage instead of percentage). */
    public var batteryVoltage: Double?
    /** Whether the pump is currently delivering a bolus. */
    public var bolusing: Bool?
    /** Whether the pump is currently in a suspended state. */
    public var suspended: Bool?
    /** Pump status string as reported by the device (e.g., \"normal\", \"suspended\", \"bolusing\"). */
    public var pumpStatus: String?
    /** Canonical closed-loop operating mode (a PumpModeState name such as \"Automatic\" or \"Manual\"), when the source reports it. */
    public var pumpMode: String?
    /** Pump internal clock time as a string (device-local time). */
    public var clock: String?
    /** Pump-reported total IOB (when no APS algorithm is running) */
    public var iob: Double?
    /** Pump-reported bolus IOB */
    public var bolusIob: Double?
    /** Catch-all for fields not mapped to dedicated columns (e.g. bolus increment) */
    public var additionalProperties: [String: JSONValue]?

    public init(timestamp: Date? = nil, utcOffset: Int? = nil, device: String? = nil, app: String? = nil, dataSource: String? = nil, syncIdentifier: String? = nil, correlationId: String? = nil, manufacturer: String? = nil, model: String? = nil, reservoir: Double? = nil, reservoirDisplay: String? = nil, batteryPercent: Int? = nil, batteryVoltage: Double? = nil, bolusing: Bool? = nil, suspended: Bool? = nil, pumpStatus: String? = nil, pumpMode: String? = nil, clock: String? = nil, iob: Double? = nil, bolusIob: Double? = nil, additionalProperties: [String: JSONValue]? = nil) {
        self.timestamp = timestamp
        self.utcOffset = utcOffset
        self.device = device
        self.app = app
        self.dataSource = dataSource
        self.syncIdentifier = syncIdentifier
        self.correlationId = correlationId
        self.manufacturer = manufacturer
        self.model = model
        self.reservoir = reservoir
        self.reservoirDisplay = reservoirDisplay
        self.batteryPercent = batteryPercent
        self.batteryVoltage = batteryVoltage
        self.bolusing = bolusing
        self.suspended = suspended
        self.pumpStatus = pumpStatus
        self.pumpMode = pumpMode
        self.clock = clock
        self.iob = iob
        self.bolusIob = bolusIob
        self.additionalProperties = additionalProperties
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case timestamp
        case utcOffset
        case device
        case app
        case dataSource
        case syncIdentifier
        case correlationId
        case manufacturer
        case model
        case reservoir
        case reservoirDisplay
        case batteryPercent
        case batteryVoltage
        case bolusing
        case suspended
        case pumpStatus
        case pumpMode
        case clock
        case iob
        case bolusIob
        case additionalProperties
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(timestamp, forKey: .timestamp)
        try container.encodeIfPresent(utcOffset, forKey: .utcOffset)
        try container.encodeIfPresent(device, forKey: .device)
        try container.encodeIfPresent(app, forKey: .app)
        try container.encodeIfPresent(dataSource, forKey: .dataSource)
        try container.encodeIfPresent(syncIdentifier, forKey: .syncIdentifier)
        try container.encodeIfPresent(correlationId, forKey: .correlationId)
        try container.encodeIfPresent(manufacturer, forKey: .manufacturer)
        try container.encodeIfPresent(model, forKey: .model)
        try container.encodeIfPresent(reservoir, forKey: .reservoir)
        try container.encodeIfPresent(reservoirDisplay, forKey: .reservoirDisplay)
        try container.encodeIfPresent(batteryPercent, forKey: .batteryPercent)
        try container.encodeIfPresent(batteryVoltage, forKey: .batteryVoltage)
        try container.encodeIfPresent(bolusing, forKey: .bolusing)
        try container.encodeIfPresent(suspended, forKey: .suspended)
        try container.encodeIfPresent(pumpStatus, forKey: .pumpStatus)
        try container.encodeIfPresent(pumpMode, forKey: .pumpMode)
        try container.encodeIfPresent(clock, forKey: .clock)
        try container.encodeIfPresent(iob, forKey: .iob)
        try container.encodeIfPresent(bolusIob, forKey: .bolusIob)
        try container.encodeIfPresent(additionalProperties, forKey: .additionalProperties)
    }
}

