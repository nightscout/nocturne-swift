//
// ResetCursorRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Request body for resetting a connector&#39;s sync cursor and re-pulling history. */
public struct ResetCursorRequest: Sendable, Codable, Hashable {

    /** Optional lower bound for the re-pull. When null, no lower bound is applied and all available history is re-ingested. */
    public var from: Date?
    /** Optional set of data types to reset. When null or empty, every data type the connector supports is re-pulled. */
    public var dataTypes: [SyncDataType]?

    public init(from: Date? = nil, dataTypes: [SyncDataType]? = nil) {
        self.from = from
        self.dataTypes = dataTypes
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case from
        case dataTypes
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(from, forKey: .from)
        try container.encodeIfPresent(dataTypes, forKey: .dataTypes)
    }
}

