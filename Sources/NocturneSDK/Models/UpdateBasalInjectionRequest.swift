//
// UpdateBasalInjectionRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Request body for updating an existing basal insulin injection record via the V4 API. The injection is identified by the route-level ID parameter. */
public struct UpdateBasalInjectionRequest: Sendable, Codable, Hashable {

    /** When the basal insulin was injected. Cannot be more than 5 minutes in the future. */
    public var timestamp: Date?
    /** UTC offset in minutes at the time of the event, for local-time display. */
    public var utcOffset: Int?
    /** Identifier of the device used to record the injection. */
    public var device: String?
    /** Name of the application that submitted this record. */
    public var app: String?
    /** Upstream data source identifier; required when SyncIdentifier is supplied. */
    public var dataSource: String?
    /** Upstream sync identifier for deduplication, paired with DataSource. */
    public var syncIdentifier: String?
    /** Reference to the PatientInsulin used for this injection. The referenced insulin's role must be Basal or Both. The server resolves this to a TreatmentInsulinContext snapshot at write time. */
    public var patientInsulinId: String?
    /** Insulin units injected. Must be greater than zero. */
    public var units: Double?
    /** Optional free-text user note. */
    public var notes: String?
    /** Correlation identifier for grouping related events. */
    public var correlationId: String?

    public init(timestamp: Date? = nil, utcOffset: Int? = nil, device: String? = nil, app: String? = nil, dataSource: String? = nil, syncIdentifier: String? = nil, patientInsulinId: String? = nil, units: Double? = nil, notes: String? = nil, correlationId: String? = nil) {
        self.timestamp = timestamp
        self.utcOffset = utcOffset
        self.device = device
        self.app = app
        self.dataSource = dataSource
        self.syncIdentifier = syncIdentifier
        self.patientInsulinId = patientInsulinId
        self.units = units
        self.notes = notes
        self.correlationId = correlationId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case timestamp
        case utcOffset
        case device
        case app
        case dataSource
        case syncIdentifier
        case patientInsulinId
        case units
        case notes
        case correlationId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(timestamp, forKey: .timestamp)
        try container.encodeIfPresent(utcOffset, forKey: .utcOffset)
        try container.encodeIfPresent(device, forKey: .device)
        try container.encodeIfPresent(app, forKey: .app)
        try container.encodeIfPresent(dataSource, forKey: .dataSource)
        try container.encodeIfPresent(syncIdentifier, forKey: .syncIdentifier)
        try container.encodeIfPresent(patientInsulinId, forKey: .patientInsulinId)
        try container.encodeIfPresent(units, forKey: .units)
        try container.encodeIfPresent(notes, forKey: .notes)
        try container.encodeIfPresent(correlationId, forKey: .correlationId)
    }
}

